'use client';

/**
 * app/calendar/page.tsx — calendar view stub.
 *
 * TODO (Person E — feat/member-e-templates-export-calendar):
 *   Replace this with the full calendar grid UI.
 *   See PRPs/10-calendar-view.md.
 *
 * middleware.ts already protects this route — you are authenticated here.
 */

export default function CalendarPage() {
  return (
    <main className="min-h-screen flex items-center justify-center">
      <p className="text-gray-400 text-sm">
        Calendar view — Person E will implement this (feat/member-e-templates-export-calendar).
      </p>
    </main>
  );
}
